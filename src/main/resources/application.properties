# --- PostgreSQL: test "Digital" DB ---
spring.datasource.url=jdbc:postgresql://10.10.14.125:5432/Digital
spring.datasource.username=postgres
spring.datasource.password=P@ssw0rd
spring.datasource.driver-class-name=org.postgresql.Driver

# We are mapping to existing tables ("Screen", "Schedule", "MediaAsset", "Dimension")
# DO NOT let Hibernate create or change them
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl


spring.h2.console.enabled=false
